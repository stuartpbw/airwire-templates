{
  "name": "airwire-marketing-templates",
  "private": true,
  "version": "0.0.1",
  "description": "High-quality landing page templates for marketers.",
  "author": "Stuart Blanton Williams",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf _site",
    "build": "npm run clean && npx @11ty/eleventy",
    "start": "npm run build -- --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0",
    "@11ty/eleventy-plugin-webc": "^0.9.0"
  }
}
