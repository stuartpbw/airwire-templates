{
  "name": "11ty-webc-starter",
  "private": true,
  "version": "0.0.1",
  "description": "A minimal 11ty WebC starter.",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf _site",
    "build": "npm run clean && npx @11ty/eleventy",
    "start": "npm run build -- --serve"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0",
    "@11ty/eleventy-plugin-webc": "^0.9.0"
  }
}
